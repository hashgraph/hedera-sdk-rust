version: "3"

includes:
    ":rust":
        taskfile: ../rust/Taskfile.yml
        dir: ../rust

tasks:
    build:
        deps: [":rust:build"]
        cmds:
            - ./gradlew build

    example:
        deps: [":rust:build"]
        cmds:
            - ./gradlew -q :examples:run{{.CLI_ARGS}}
